const twoSum = (arr, goal) => {
    if (!arr || !goal || arr.length < 2) {
        return false
    }
    let sortedArr = arr.sort() 
    let aI = 0
    let bI = sortedArr.length - 1
    while (aI !== bI) {
        
    }
    return []
}



module.exports = twoSum